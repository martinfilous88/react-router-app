import { Paper } from "@mui/material"
export default function Home() {

    return (
        <Paper variant="elevation" elevation={4} square={false} style={{ textAlign: "center" }}>
        <h1>Vítejte v mém react projektu.</h1>
        </Paper>
    )
};